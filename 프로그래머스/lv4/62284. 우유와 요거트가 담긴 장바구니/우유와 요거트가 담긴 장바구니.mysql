-- 코드를 입력하세요
SELECT A.CART_ID
FROM (
    SELECT ID, CART_ID
    FROM CART_PRODUCTS
    WHERE NAME = 'MILK'
) AS A
INNER JOIN (
    SELECT ID, CART_ID
    FROM CART_PRODUCTS
    WHERE NAME = 'YOGURT'
) AS B
ON A.CART_ID = B.CART_ID